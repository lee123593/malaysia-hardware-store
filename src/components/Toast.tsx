"use client";

// Toast is now handled entirely by ToastProvider.
// This file kept as a re-export for backwards compatibility with any remaining direct imports.
export { useToast } from "@/components/ToastProvider";
